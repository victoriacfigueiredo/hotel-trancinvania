// src/theme.ts
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    // heading: `'MyCustomFont', 'Inter', sans-serif`,
    body: `'Inter', sans-serif`,
  },
});

export default theme;
